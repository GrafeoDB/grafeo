//! Logical query planner.
//!
//! Converts a bound AST into a logical plan.

/// Placeholder for planner implementation.
pub struct Planner {
    // TODO: Add implementation
}

impl Planner {
    /// Creates a new planner.
    #[must_use]
    pub fn new() -> Self {
        Self {}
    }
}

impl Default for Planner {
    fn default() -> Self {
        Self::new()
    }
}
